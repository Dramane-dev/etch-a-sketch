let container = document.getElementById('container');
let reset = document.getElementById('reset');
let progressBar = document.querySelector('#size');
let multicolors = document.getElementById('multicolors');
let multicolorsObj = {
    isMulticolors: false,
};
let columns = 16;
let rows = 16;

export {
    container,
    reset,
    progressBar,
    multicolors,
    multicolorsObj,
    columns,
    rows
}